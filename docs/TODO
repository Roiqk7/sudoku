1. Make working scenes
        1. Finish game scene and all its components
           1. Add timer
           2. Finish selected number highlight
           3. Fix regular highlight
           4. Simplify code
              1. Simplify the number panel click function
        2. Fix bugs (music stopped playing, game crashes etc.)
           1. Music stop bug (may be fixed)
           2. Game crash bug when double hint
           3. Weird controls (not intuitive)
           4. When first cell select and then number select it behaves weird
2. Add more tests
   1. Rewrite the tests for the updated Grid